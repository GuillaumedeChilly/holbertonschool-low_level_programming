#include "main.h"

/**
 * _isdigit - Check if a character is a digit (0-9)
 * @c: character code to test
 *
 * Return: 1 if digit, 0 otherwise.
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
