# C - malloc, free

This project contains exercises about dynamic memory allocation in C:

0. create_array - create and initialize a char array
1. _strdup      - duplicate a string
2. str_concat   - concatenate two strings
3. alloc_grid   - allocate a 2D array of ints
4. free_grid    - free a 2D array of ints
